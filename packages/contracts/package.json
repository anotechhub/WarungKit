{
  "name": "@warungkit/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Zod schemas and TypeScript types for WarungKit frontend and backend.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"test: no test suite yet for @warungkit/contracts (schema-only package)\""
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
